# Rule 1 - 每則 Scenario 必須有分類小標的「對應欄位」

- Level: `MUST`
- 每則 Scenario 在 Gherkin 區塊之後必須有 `**對應欄位**:` 標題。
- 必須使用分類小標再條列：至少含 **US**、**AC / Edge**、**FR**（無獨立 FR 時可在 FR 下註明原因）。
- 後端區塊另需 **API**；前端區塊另需 **UI**；整合區塊另需 **前端** 與 **後端**（分別條列 UI 與 API）。
- 條列格式為縮排 `- <ID 或路徑> <說明>`；不可只留裸編號。

## Good Example

- 這個例子是好的，因為小標齊全且後端含 API。

```md
**對應欄位**:

- **US**
  - US-1 …
- **AC / Edge**
  - AC-1-1 …
- **FR**
  - US1-FR1 …
- **API**
  - `POST /albums` …
```

## Bad Example

- 這個例子是壞的，因為扁平一列、沒有分類小標，也沒有 API／UI。

```md
**對應欄位**:

- US-1 …
- AC-1-1 …
```

# Rule 2 - 檔末必須有測試摘要總表，一列一條 AC 或 Edge

- Level: `MUST`
- 檔案必須包含 `## 測試摘要總表`。
- 表格欄位固定為 `User Story | AC / Edge | Scenario | 後端 | 前端 | 整合`。
- 一列只對一條 AC 或一條 Edge；同一 Scenario 對到多條時拆成多列（Scenario 與 ✓／— 可相同）。
- 落點欄以 `✓` 或 `—` 標示該證明區塊是否有寫入對應 Scenario；FR 不列入本表。

## Good Example

- 這個例子是好的，因為一列一條 AC／Edge，並標明三區落點。

```md
| User Story | AC / Edge | Scenario | 後端 | 前端 | 整合 |
| --- | --- | --- | --- | --- | --- |
| US-1 | AC-1-2 | S-1-2 | ✓ | ✓ | ✓ |
| US-1 | Edge-G-2 | S-1-2 | ✓ | ✓ | ✓ |
```

## Bad Example

- 這個例子是壞的，因為把多個 Scenario 併一列，且缺少 AC／Edge 欄。

```md
| Scenario | 後端 | 前端 | 整合 |
| --- | --- | --- | --- |
| S-1-1～S-1-5 | ✓ | ✓ | — |
```

# Rule 3 - 不可再使用「元素覆蓋總表」取代測試摘要總表

- Level: `MUST`
- 檔末落點總覽以「測試摘要總表」為準，不產出「元素覆蓋總表」。
- FR 覆蓋只保留在各 Scenario 的對應欄位，不另開 FR 總表。

## Good Example

- 這個例子是好的，因為檔末只有測試摘要總表與假設。

```md
## 測試摘要總表
…

## 假設
…
```

## Bad Example

- 這個例子是壞的，因為仍保留舊的元素覆蓋總表並與摘要表重複。

```md
## 元素覆蓋總表
| ID | 描述 | Scenario |
```

# Rule 4 - 檔末必須有 blocked 章節與假設章節

- Level: `MUST`
- 必須包含 `## 未產出 Scenario 的邊界（blocked）`；無 blocked 項時表列仍寫 `| （無） | （無） | （無） |`。
- 必須包含 `## 假設`，且為檔案最後一個二級標題；其下至少一條 `- ` 假設。

## Good Example

- 這個例子是好的，因為 blocked 與假設齊備，假設在最後。

```md
## 未產出 Scenario 的邊界（blocked）

| ID | 描述 | 阻塞原因 |
| --- | --- | --- |
| （無） | （無） | （無） |

## 測試摘要總表
…

## 假設

- …
```

## Bad Example

- 這個例子是壞的，因為省略假設或把假設插在摘要表之前。

```md
## 假設
- …

## 測試摘要總表
…
```
