# 端對端測試計畫：照片相簿整理應用程式

**功能分支**: `001-photo-albums`  
**建立日期**: 2026-07-22  
**狀態**: 草稿  

---

## 後端

### US-1 建立相簿並整理照片（優先級：P1）

#### Scenario: S-1-1 建立「旅行」相簿並一次匯入多格式照片

```gherkin
Scenario: 建立「旅行」相簿並一次匯入多格式照片
  Given 使用者尚未建立任何相簿
  And 本機有可選的 JPEG、PNG 與 WebP 照片
  When 使用者建立一個名為「旅行」的相簿並一次將這些照片加入「旅行」相簿
  Then 「旅行」相簿存在
  And 該相簿內含剛加入的那些照片
```

**對應欄位**:

- **US**
  - US-1 建立相簿並整理照片（優先級：P1）— 使用者可以建立多個相簿，將照片放入對應相簿中，讓原本零散的照片依主題或用途被整理起來。
- **AC / Edge**
  - AC-1-1 假設使用者尚未建立任何相簿，當建立一個名為「旅行」的相簿並一次匯入多張 JPEG、PNG 與 WebP 照片，則系統應顯示「旅行」相簿且內含這些照片
- **FR**
  - US1-FR1 系統必須允許使用者建立相簿並命名
  - US1-FR2 系統必須允許使用者將照片加入指定相簿
  - US1-FR3 系統必須支援一次選取多張 JPEG、PNG 或 WebP 照片匯入，並加入指定相簿
- **API**
  - `POST /albums` 建立相簿
  - `POST /albums/:albumId/photos` 批次上傳 JPEG／PNG／WebP
  - `GET /albums` 確認相簿存在
  - `GET /albums/:albumId/photos` 確認相簿內含剛加入的照片

---



#### Scenario: S-1-2 將已歸屬照片改加入另一相簿後只留在目標相簿

```gherkin
Scenario: 將已歸屬照片改加入另一相簿後只留在目標相簿
  Given 使用者已建立相簿 A 與相簿 B
  And 某張照片已屬於相簿 A
  When 使用者將該照片加入相簿 B
  Then 該照片只出現在相簿 B
  And 該照片不再出現在相簿 A
```

**對應欄位**:

- **US**
  - US-1 建立相簿並整理照片（優先級：P1）— 使用者可以建立多個相簿，將照片放入對應相簿中，讓原本零散的照片依主題或用途被整理起來。
- **AC / Edge**
  - AC-1-2 假設使用者已建立多個相簿且某張照片已屬於相簿 A，當使用者將該照片加入相簿 B，則該照片應只出現在相簿 B，且不再出現在相簿 A
  - Edge-G-2 當系統偵測到會造成同一張照片同時屬於多個相簿的操作時，必須改為將該照片移動到目標相簿，不得保留多重歸屬
- **FR**
  - US1-FR4 當使用者將已屬於某相簿的照片加入另一相簿時，系統必須把該照片從原相簿移出，使其同一時間只屬於一個相簿
- **API**
  - `PATCH /photos/:id` 覆寫目標 `album_id`（移動）
  - `GET /albums/:albumId/photos` 分別對相簿 A、B 斷言歸屬（只在 B、不在 A）
- **Data**
  - data-plan：同一照片同一時間只屬於一個相簿（移動覆寫歸屬）
  - DDL：`photos.album_id` 單外鍵語意（非多對多），移動後舊相簿列表不再含該列

---



#### Scenario: S-1-3 空相簿尚未加入照片時仍應顯示

```gherkin
Scenario: 空相簿尚未加入照片時仍應顯示
  Given 使用者尚未建立任何相簿
  When 使用者建立一個名為「草稿」的相簿且尚未加入任何照片
  Then 「草稿」相簿仍被顯示
```

**對應欄位**:

- **US**
  - US-1 建立相簿並整理照片（優先級：P1）— 使用者可以建立多個相簿，將照片放入對應相簿中，讓原本零散的照片依主題或用途被整理起來。
- **AC / Edge**
  - Edge-1-1 當使用者建立空相簿但尚未加入照片時，系統仍應顯示該相簿
- **FR**
  - US1-FR1 系統必須允許使用者建立相簿並命名
- **API**
  - `POST /albums` 建立空相簿
  - `GET /albums` 列表仍含該相簿（`photo_count` 可為 0）

---



#### Scenario: S-1-4 選取不支援格式時拒絕匯入並說明支援範圍

```gherkin
Scenario: 選取不支援格式時拒絕匯入並說明支援範圍
  Given 使用者已建立一個相簿
  And 本機有至少一個非 JPEG、PNG、WebP 的檔案
  When 使用者嘗試將該不支援格式的檔案加入該相簿
  Then 系統拒絕匯入
  And 說明僅支援 JPEG、PNG、WebP
```

**對應欄位**:

- **US**
  - US-1 建立相簿並整理照片（優先級：P1）— 使用者可以建立多個相簿，將照片放入對應相簿中，讓原本零散的照片依主題或用途被整理起來。
- **AC / Edge**
  - Edge-1-2 當使用者選取不支援的檔案格式時，系統應拒絕匯入並說明僅支援 JPEG、PNG、WebP
- **FR**
  - US1-FR3 系統必須支援一次選取多張 JPEG、PNG 或 WebP 照片匯入，並加入指定相簿
- **API**
  - `POST /albums/:albumId/photos` 不支援格式應拒絕（如 415），錯誤訊息說明僅支援 JPEG、PNG、WebP

---



#### Scenario: S-1-5 拒絕相簿巢狀並維持單層

```gherkin
Scenario: 拒絕相簿巢狀並維持單層
  Given 系統中已存在至少兩個相簿
  When 使用者嘗試把一個相簿放進另一個相簿，或在相簿下建立子相簿
  Then 系統阻止造成巢狀結構的行為
  And 所有相簿仍維持單層
```

**對應欄位**:

- **US**
  - US-1 建立相簿並整理照片（優先級：P1）— 使用者可以建立多個相簿，將照片放入對應相簿中，讓原本零散的照片依主題或用途被整理起來。
- **AC / Edge**
  - Edge-G-1 當使用者嘗試以任何操作把相簿放進另一個相簿中時，系統必須阻止此行為
- **FR**
  - （無獨立 FR 編號；對應 GR-001 所有相簿同一層級且不可嵌套）
- **API**
  - `POST /albums` 不提供 `parent_album_id` 或任何巢狀欄位；契約上無法建立子相簿
  - `GET /albums` 回傳之相簿結構維持單層分組，無巢狀子相簿

---



### US-2 在主頁面依日期瀏覽相簿（優先級：P2）



#### Scenario: S-2-1 主頁依相簿建立日期分組顯示

```gherkin
Scenario: 主頁依相簿建立日期分組顯示
  Given 使用者已在不同日期建立多個相簿
  When 使用者進入主頁面
  Then 系統依各相簿的建立日期將相簿分組顯示
```

**對應欄位**:

- **US**
  - US-2 在主頁面依日期瀏覽相簿（優先級：P2）— 使用者回到主頁面時，可以看到所有相簿依建立日期分組顯示，藉此更快找到特定時間建立的照片集合。
- **AC / Edge**
  - AC-2-1 假設使用者已在不同日期建立多個相簿，當使用者進入主頁面，則系統應依各相簿的建立日期將相簿分組顯示
- **FR**
  - US2-FR1 系統必須在主頁面顯示所有相簿
  - US2-FR2 系統必須依據相簿建立日期，在主頁面將相簿分組顯示
- **API**
  - `GET /albums` 回傳依 `group_date` 分組之 `groups[]`（由 `created_at` 日期部分衍生）

---



#### Scenario: S-2-2 沒有相簿的日期不顯示空白分組

```gherkin
Scenario: 沒有相簿的日期不顯示空白分組
  Given 使用者已建立若干相簿
  And 某些日期下沒有任何相簿
  When 使用者進入主頁面
  Then 系統不顯示那些沒有相簿的空白日期分組
```

**對應欄位**:

- **US**
  - US-2 在主頁面依日期瀏覽相簿（優先級：P2）— 使用者回到主頁面時，可以看到所有相簿依建立日期分組顯示，藉此更快找到特定時間建立的照片集合。
- **AC / Edge**
  - Edge-2-1 當某個日期分組下沒有任何相簿時，系統不應顯示空白日期分組
- **FR**
  - US2-FR2 系統必須依據相簿建立日期，在主頁面將相簿分組顯示
- **API**
  - `GET /albums` 不得回傳 albums 為空的日期分組

---



### US-3 透過拖放重新排列相簿（優先級：P3）



#### Scenario: S-3-2 拖放排序後重新整理或再進主頁仍保留順序

```gherkin
Scenario: 拖放排序後重新整理或再進主頁仍保留順序
  Given 使用者已在主頁完成一次相簿拖放排序
  When 使用者重新整理或再次進入主頁面
  Then 系統仍依先前保存的順序顯示相簿
```

**對應欄位**:

- **US**
  - US-3 透過拖放重新排列相簿（優先級：P3）— 使用者可以在主頁面直接拖放相簿，調整自己想要的排列順序，讓常用或重要的相簿排在更顯眼的位置。
- **AC / Edge**
  - AC-3-2 假設使用者已完成一次相簿拖放排序，當使用者重新整理或再次進入主頁面，則系統應仍依先前保存的順序顯示相簿
- **FR**
  - US3-FR2 系統必須保存使用者重新排列後的相簿順序
- **API**
  - `PATCH /albums/reorder` 持久化同分組內 `sort_order`
  - `GET /albums` 再次讀取時組內順序與重排結果一致

---



### US-4 在相簿內以平鋪方式預覽照片（優先級：P3）



#### Scenario: S-4-1 打開含多張照片的相簿以平鋪預覽顯示

```gherkin
Scenario: 打開含多張照片的相簿以平鋪預覽顯示
  Given 某個相簿內已有多張照片
  When 使用者打開該相簿
  Then 系統以平鋪式預覽顯示相簿中的照片
```

**對應欄位**:

- **US**
  - US-4 在相簿內以平鋪方式預覽照片（優先級：P3）— 使用者進入任一相簿後，可以一次看到多張照片的縮圖平鋪排列，快速掃描相簿內容。
- **AC / Edge**
  - AC-4-1 假設某個相簿內已有多張照片，當使用者打開該相簿，則系統應以平鋪式預覽顯示相簿中的照片
- **FR**
  - US4-FR1 系統必須允許使用者進入單一相簿查看內容
  - US4-FR2 系統必須在相簿內以平鋪式介面預覽照片
- **API**
  - `GET /albums/:albumId/photos` 回傳該相簿多筆照片（含預覽所需 URL 欄位）

---



#### Scenario: S-4-2 空相簿打開時顯示可理解的空狀態

```gherkin
Scenario: 空相簿打開時顯示可理解的空狀態
  Given 某個相簿內尚無照片
  When 使用者打開該相簿
  Then 系統顯示可理解的空狀態
  And 不是一片空白畫面
```

**對應欄位**:

- **US**
  - US-4 在相簿內以平鋪方式預覽照片（優先級：P3）— 使用者進入任一相簿後，可以一次看到多張照片的縮圖平鋪排列，快速掃描相簿內容。
- **AC / Edge**
  - Edge-4-1 當相簿內尚無照片時，系統應顯示可理解的空狀態，而不是空白畫面
- **FR**
  - US4-FR1 系統必須允許使用者進入單一相簿查看內容
  - US4-FR3 系統必須在相簿沒有照片時顯示可理解的空狀態
- **API**
  - `GET /albums/:albumId/photos` 空相簿回 200 與空陣列（空狀態文案由前端呈現）

---



## 前端



### US-1 建立相簿並整理照片（優先級：P1）



#### Scenario: S-1-1 建立「旅行」相簿並一次匯入多格式照片

```gherkin
Scenario: 建立「旅行」相簿並一次匯入多格式照片
  Given 使用者尚未建立任何相簿
  And 本機有可選的 JPEG、PNG 與 WebP 照片
  When 使用者建立一個名為「旅行」的相簿並一次將這些照片加入「旅行」相簿
  Then 「旅行」相簿存在
  And 該相簿內含剛加入的那些照片
```

**對應欄位**:

- **US**
  - US-1 建立相簿並整理照片（優先級：P1）— 使用者可以建立多個相簿，將照片放入對應相簿中，讓原本零散的照片依主題或用途被整理起來。
- **AC / Edge**
  - AC-1-1 假設使用者尚未建立任何相簿，當建立一個名為「旅行」的相簿並一次匯入多張 JPEG、PNG 與 WebP 照片，則系統應顯示「旅行」相簿且內含這些照片
- **FR**
  - US1-FR1 系統必須允許使用者建立相簿並命名
  - US1-FR2 系統必須允許使用者將照片加入指定相簿
  - US1-FR3 系統必須支援一次選取多張 JPEG、PNG 或 WebP 照片匯入，並加入指定相簿
- **UI**
  - 頁面：主頁（Home）— 建立相簿表單（Modal）
  - 頁面：相簿詳情（Album）— 批次選取本機照片上傳、平鋪區顯示已加入照片
  - 業務邏輯 1：建立相簿並加入照片

---



#### Scenario: S-1-2 將已歸屬照片改加入另一相簿後只留在目標相簿

```gherkin
Scenario: 將已歸屬照片改加入另一相簿後只留在目標相簿
  Given 使用者已建立相簿 A 與相簿 B
  And 某張照片已屬於相簿 A
  When 使用者將該照片加入相簿 B
  Then 該照片只出現在相簿 B
  And 該照片不再出現在相簿 A
```

**對應欄位**:

- **US**
  - US-1 建立相簿並整理照片（優先級：P1）— 使用者可以建立多個相簿，將照片放入對應相簿中，讓原本零散的照片依主題或用途被整理起來。
- **AC / Edge**
  - AC-1-2 假設使用者已建立多個相簿且某張照片已屬於相簿 A，當使用者將該照片加入相簿 B，則該照片應只出現在相簿 B，且不再出現在相簿 A
  - Edge-G-2 當系統偵測到會造成同一張照片同時屬於多個相簿的操作時，必須改為將該照片移動到目標相簿，不得保留多重歸屬
- **FR**
  - US1-FR4 當使用者將已屬於某相簿的照片加入另一相簿時，系統必須把該照片從原相簿移出，使其同一時間只屬於一個相簿
- **UI**
  - 頁面：相簿詳情（Album）—「移至其他相簿」附屬操作與目標清單
  - 業務邏輯 4：跨相簿移動照片後，原相簿平鋪區不再顯示該照片

---



#### Scenario: S-1-3 空相簿尚未加入照片時仍應顯示

```gherkin
Scenario: 空相簿尚未加入照片時仍應顯示
  Given 使用者尚未建立任何相簿
  When 使用者建立一個名為「草稿」的相簿且尚未加入任何照片
  Then 「草稿」相簿仍被顯示
```

**對應欄位**:

- **US**
  - US-1 建立相簿並整理照片（優先級：P1）— 使用者可以建立多個相簿，將照片放入對應相簿中，讓原本零散的照片依主題或用途被整理起來。
- **AC / Edge**
  - Edge-1-1 當使用者建立空相簿但尚未加入照片時，系統仍應顯示該相簿
- **FR**
  - US1-FR1 系統必須允許使用者建立相簿並命名
- **UI**
  - 頁面：主頁（Home）— 日期分組列表仍顯示 `photo_count` 為 0 的相簿項目

---



#### Scenario: S-1-4 選取不支援格式時拒絕匯入並說明支援範圍

```gherkin
Scenario: 選取不支援格式時拒絕匯入並說明支援範圍
  Given 使用者已建立一個相簿
  And 本機有至少一個非 JPEG、PNG、WebP 的檔案
  When 使用者嘗試將該不支援格式的檔案加入該相簿
  Then 系統拒絕匯入
  And 說明僅支援 JPEG、PNG、WebP
```

**對應欄位**:

- **US**
  - US-1 建立相簿並整理照片（優先級：P1）— 使用者可以建立多個相簿，將照片放入對應相簿中，讓原本零散的照片依主題或用途被整理起來。
- **AC / Edge**
  - Edge-1-2 當使用者選取不支援的檔案格式時，系統應拒絕匯入並說明僅支援 JPEG、PNG、WebP
- **FR**
  - US1-FR3 系統必須支援一次選取多張 JPEG、PNG 或 WebP 照片匯入，並加入指定相簿
- **UI**
  - 頁面：相簿詳情（Album）— 上傳失敗時顯示錯誤，並說明僅支援 JPEG、PNG、WebP

---



#### Scenario: S-1-5 拒絕相簿巢狀並維持單層

```gherkin
Scenario: 拒絕相簿巢狀並維持單層
  Given 系統中已存在至少兩個相簿
  When 使用者嘗試把一個相簿放進另一個相簿，或在相簿下建立子相簿
  Then 系統阻止造成巢狀結構的行為
  And 所有相簿仍維持單層
```

**對應欄位**:

- **US**
  - US-1 建立相簿並整理照片（優先級：P1）— 使用者可以建立多個相簿，將照片放入對應相簿中，讓原本零散的照片依主題或用途被整理起來。
- **AC / Edge**
  - Edge-G-1 當使用者嘗試以任何操作把相簿放進另一個相簿中時，系統必須阻止此行為
- **FR**
  - （無獨立 FR 編號；對應 GR-001 所有相簿同一層級且不可嵌套）
- **UI**
  - 頁面：主頁（Home）— 不提供「把相簿拖進另一相簿」的有效投放目標；無在相簿下建立子相簿的操作入口

---



### US-2 在主頁面依日期瀏覽相簿（優先級：P2）



#### Scenario: S-2-1 主頁依相簿建立日期分組顯示

```gherkin
Scenario: 主頁依相簿建立日期分組顯示
  Given 使用者已在不同日期建立多個相簿
  When 使用者進入主頁面
  Then 系統依各相簿的建立日期將相簿分組顯示
```

**對應欄位**:

- **US**
  - US-2 在主頁面依日期瀏覽相簿（優先級：P2）— 使用者回到主頁面時，可以看到所有相簿依建立日期分組顯示，藉此更快找到特定時間建立的照片集合。
- **AC / Edge**
  - AC-2-1 假設使用者已在不同日期建立多個相簿，當使用者進入主頁面，則系統應依各相簿的建立日期將相簿分組顯示
- **FR**
  - US2-FR1 系統必須在主頁面顯示所有相簿
  - US2-FR2 系統必須依據相簿建立日期，在主頁面將相簿分組顯示
- **UI**
  - 頁面：主頁（Home）— 依 `group_date` 分組呈現，每組顯示日期標題
  - 業務邏輯 2：主頁依建立日期分組瀏覽相簿

---



#### Scenario: S-2-2 沒有相簿的日期不顯示空白分組

```gherkin
Scenario: 沒有相簿的日期不顯示空白分組
  Given 使用者已建立若干相簿
  And 某些日期下沒有任何相簿
  When 使用者進入主頁面
  Then 系統不顯示那些沒有相簿的空白日期分組
```

**對應欄位**:

- **US**
  - US-2 在主頁面依日期瀏覽相簿（優先級：P2）— 使用者回到主頁面時，可以看到所有相簿依建立日期分組顯示，藉此更快找到特定時間建立的照片集合。
- **AC / Edge**
  - Edge-2-1 當某個日期分組下沒有任何相簿時，系統不應顯示空白日期分組
- **FR**
  - US2-FR2 系統必須依據相簿建立日期，在主頁面將相簿分組顯示
- **UI**
  - 頁面：主頁（Home）— 不渲染 albums 為空的日期分組標題

---



### US-3 透過拖放重新排列相簿（優先級：P3）



#### Scenario: S-3-1 拖放後主頁立即依新順序顯示相簿

```gherkin
Scenario: 拖放後主頁立即依新順序顯示相簿
  Given 主頁面上已有多個相簿
  When 使用者將其中一個相簿拖放到新的位置
  Then 系統依新的順序顯示相簿
```

**對應欄位**:

- **US**
  - US-3 透過拖放重新排列相簿（優先級：P3）— 使用者可以在主頁面直接拖放相簿，調整自己想要的排列順序，讓常用或重要的相簿排在更顯眼的位置。
- **AC / Edge**
  - AC-3-1 假設主頁面上已有多個相簿，當使用者將其中一個相簿拖放到新的位置，則系統應依新的順序顯示相簿
- **FR**
  - US3-FR1 使用者必須能夠在主頁面透過拖放重新排列相簿順序
- **UI**
  - 頁面：主頁（Home）— 同 `group_date` 分組內拖放後立即更新組內 DOM／畫面順序
  - 業務邏輯 3：同建立日分組內拖放重排相簿

---



#### Scenario: S-3-2 拖放排序後重新整理或再進主頁仍保留順序

```gherkin
Scenario: 拖放排序後重新整理或再進主頁仍保留順序
  Given 使用者已在主頁完成一次相簿拖放排序
  When 使用者重新整理或再次進入主頁面
  Then 系統仍依先前保存的順序顯示相簿
```

**對應欄位**:

- **US**
  - US-3 透過拖放重新排列相簿（優先級：P3）— 使用者可以在主頁面直接拖放相簿，調整自己想要的排列順序，讓常用或重要的相簿排在更顯眼的位置。
- **AC / Edge**
  - AC-3-2 假設使用者已完成一次相簿拖放排序，當使用者重新整理或再次進入主頁面，則系統應仍依先前保存的順序顯示相簿
- **FR**
  - US3-FR2 系統必須保存使用者重新排列後的相簿順序
- **UI**
  - 頁面：主頁（Home）— 重新載入後依回傳之 `sort_order` 呈現組內順序（本區可用 Mock 餵已重排資料驗證呈現）

---



#### Scenario: S-3-3 僅一個相簿時拖放不造成錯誤或混淆

```gherkin
Scenario: 僅一個相簿時拖放不造成錯誤或混淆
  Given 主頁面只有一個相簿
  When 使用者對該相簿進行拖放操作
  Then 系統不出現錯誤
  And 不造成混淆的互動結果
```

**對應欄位**:

- **US**
  - US-3 透過拖放重新排列相簿（優先級：P3）— 使用者可以在主頁面直接拖放相簿，調整自己想要的排列順序，讓常用或重要的相簿排在更顯眼的位置。
- **AC / Edge**
  - Edge-3-1 當主頁面只有一個相簿時，拖放操作不應造成錯誤或混淆的互動結果
- **FR**
  - US3-FR1 使用者必須能夠在主頁面透過拖放重新排列相簿順序
- **UI**
  - 頁面：主頁（Home）— 僅一個相簿的分組內，拖放不造成錯誤或混淆互動

---



### US-4 在相簿內以平鋪方式預覽照片（優先級：P3）



#### Scenario: S-4-1 打開含多張照片的相簿以平鋪預覽顯示

```gherkin
Scenario: 打開含多張照片的相簿以平鋪預覽顯示
  Given 某個相簿內已有多張照片
  When 使用者打開該相簿
  Then 系統以平鋪式預覽顯示相簿中的照片
```

**對應欄位**:

- **US**
  - US-4 在相簿內以平鋪方式預覽照片（優先級：P3）— 使用者進入任一相簿後，可以一次看到多張照片的縮圖平鋪排列，快速掃描相簿內容。
- **AC / Edge**
  - AC-4-1 假設某個相簿內已有多張照片，當使用者打開該相簿，則系統應以平鋪式預覽顯示相簿中的照片
- **FR**
  - US4-FR1 系統必須允許使用者進入單一相簿查看內容
  - US4-FR2 系統必須在相簿內以平鋪式介面預覽照片
- **UI**
  - 頁面：相簿詳情（Album）— 有照片時以平鋪縮圖網格呈現
  - 業務邏輯 4：相簿內平鋪預覽

---



#### Scenario: S-4-2 空相簿打開時顯示可理解的空狀態

```gherkin
Scenario: 空相簿打開時顯示可理解的空狀態
  Given 某個相簿內尚無照片
  When 使用者打開該相簿
  Then 系統顯示可理解的空狀態
  And 不是一片空白畫面
```

**對應欄位**:

- **US**
  - US-4 在相簿內以平鋪方式預覽照片（優先級：P3）— 使用者進入任一相簿後，可以一次看到多張照片的縮圖平鋪排列，快速掃描相簿內容。
- **AC / Edge**
  - Edge-4-1 當相簿內尚無照片時，系統應顯示可理解的空狀態，而不是空白畫面
- **FR**
  - US4-FR1 系統必須允許使用者進入單一相簿查看內容
  - US4-FR3 系統必須在相簿沒有照片時顯示可理解的空狀態
- **UI**
  - 頁面：相簿詳情（Album）— 無照片時顯示可理解空狀態（提示可上傳本機照片）

---



## 整合

> 僅收 Then 必須前後端真串接才有意義的 Scenario（本輪：跨相簿移動後兩邊一致、拖放後重整仍保留順序）。



### US-1 建立相簿並整理照片（優先級：P1）



#### Scenario: S-1-2 將已歸屬照片改加入另一相簿後只留在目標相簿

```gherkin
Scenario: 將已歸屬照片改加入另一相簿後只留在目標相簿
  Given 使用者已建立相簿 A 與相簿 B
  And 某張照片已屬於相簿 A
  When 使用者將該照片加入相簿 B
  Then 該照片只出現在相簿 B
  And 該照片不再出現在相簿 A
```

**對應欄位**:

- **US**
  - US-1 建立相簿並整理照片（優先級：P1）— 使用者可以建立多個相簿，將照片放入對應相簿中，讓原本零散的照片依主題或用途被整理起來。
- **AC / Edge**
  - AC-1-2 假設使用者已建立多個相簿且某張照片已屬於相簿 A，當使用者將該照片加入相簿 B，則該照片應只出現在相簿 B，且不再出現在相簿 A
  - Edge-G-2 當系統偵測到會造成同一張照片同時屬於多個相簿的操作時，必須改為將該照片移動到目標相簿，不得保留多重歸屬
- **FR**
  - US1-FR4 當使用者將已屬於某相簿的照片加入另一相簿時，系統必須把該照片從原相簿移出，使其同一時間只屬於一個相簿
- **前端**
  - 頁面：相簿詳情（Album）—「移至其他相簿」；移動後於 A／B 相簿詳情平鋪區驗證可見性
  - 業務邏輯 4：跨相簿移動照片
- **後端**
  - `PATCH /photos/:id` 移動歸屬
  - `GET /albums/:albumId/photos` 對 A、B 斷言只在 B

---



### US-3 透過拖放重新排列相簿（優先級：P3）



#### Scenario: S-3-2 拖放排序後重新整理或再進主頁仍保留順序

```gherkin
Scenario: 拖放排序後重新整理或再進主頁仍保留順序
  Given 使用者已在主頁完成一次相簿拖放排序
  When 使用者重新整理或再次進入主頁面
  Then 系統仍依先前保存的順序顯示相簿
```

**對應欄位**:

- **US**
  - US-3 透過拖放重新排列相簿（優先級：P3）— 使用者可以在主頁面直接拖放相簿，調整自己想要的排列順序，讓常用或重要的相簿排在更顯眼的位置。
- **AC / Edge**
  - AC-3-2 假設使用者已完成一次相簿拖放排序，當使用者重新整理或再次進入主頁面，則系統應仍依先前保存的順序顯示相簿
- **FR**
  - US3-FR2 系統必須保存使用者重新排列後的相簿順序
- **前端**
  - 頁面：主頁（Home）— 同分組內拖放後，重新整理或再進主頁仍見新順序
  - 業務邏輯 3：拖放重排並持久化
- **後端**
  - `PATCH /albums/reorder` 寫入順序
  - `GET /albums` 重整後讀回順序一致

---



## 未產出 Scenario 的邊界（blocked）

本期 `spec.md` 邊界條件無 `[NEEDS CLARIFICATION]`，亦無依賴未澄清決策的邊界；本輪無 blocked 項。


| ID  | 描述  | 阻塞原因 |
| --- | --- | ---- |
| （無） | （無） | （無）  |


---



## 測試摘要總表

> 一列一條 AC 或一條 Edge。同一 Scenario 對到多條 AC／Edge 時拆成多列（Scenario 與落點相同）。FR 僅見各 Scenario「對應欄位」，不列入本表。`✓`＝該證明區塊有寫入 Scenario；`—`＝未寫入。

| User Story | AC / Edge | Scenario | 後端 | 前端 | 整合 |
| --- | --- | --- | --- | --- | --- |
| US-1 | AC-1-1 | S-1-1 | ✓ | ✓ | — |
| US-1 | AC-1-2 | S-1-2 | ✓ | ✓ | ✓ |
| US-1 | Edge-G-2 | S-1-2 | ✓ | ✓ | ✓ |
| US-1 | Edge-1-1 | S-1-3 | ✓ | ✓ | — |
| US-1 | Edge-1-2 | S-1-4 | ✓ | ✓ | — |
| US-1 | Edge-G-1 | S-1-5 | ✓ | ✓ | — |
| US-2 | AC-2-1 | S-2-1 | ✓ | ✓ | — |
| US-2 | Edge-2-1 | S-2-2 | ✓ | ✓ | — |
| US-3 | AC-3-1 | S-3-1 | — | ✓ | — |
| US-3 | AC-3-2 | S-3-2 | ✓ | ✓ | ✓ |
| US-3 | Edge-3-1 | S-3-3 | — | ✓ | — |
| US-4 | AC-4-1 | S-4-1 | ✓ | ✓ | — |
| US-4 | Edge-4-1 | S-4-2 | ✓ | ✓ | — |

---

## 假設

- 本計畫依同 package 的 `spec.md` 展開 Scenario，並對齊 `system-analyze/api-plan.md`、`system-analyze/ui-plan.md`；若存在亦對齊 `data-plan.md`／`DDL.md` 可測持久化約束。GWT 仍使用領域語言，endpoint／頁面／資料約束細節只寫在對應欄位（含可選 **Data**）。
- 證明區塊：`## 後端`（API → Response／DB）、`## 前端`（瀏覽器 UI，後端可 Mock）、`## 整合`（前後端真串接）；Scenario 標題僅用業務 ID（如 `S-1-1`），證明方式由所在 `##` 區塊區分。
- 落點：AC／Edge 前後端都碰得到則後端＋前端都寫；整合僅當 Then 必須真串接才有意義（本輪為 S-1-2、S-3-2）。
- 檔末「測試摘要總表」以一列一條 AC／Edge 標示落點；FR 只留在各 Scenario 對應欄位。
- Gherkin 慣例：`Given`／`Then` 可用 `And` 延續；`When` 僅一條操作步驟（不在 When 後接 And）。
- AC 與全域邊界語意重複時合併為單一 Scenario，並在「AC / Edge」同時列出（見 S-1-2）；摘要表仍拆成兩列。
- 本期無 `[NEEDS CLARIFICATION]` 邊界，故無 blocked Scenario。
- Edge ID（Edge-x-y／Edge-G-y）為本計畫為追溯穩定而編號；Edge-x-y 對應各 US「邊界情境」條列順序，Edge-G-y 對應「全域邊界情境」條列順序。
- S-3-1、S-3-3 主要落點為互動呈現，本輪僅寫前端；S-3-2 才涵蓋順序持久化之後端與整合。
- 拖放僅限同 `group_date` 分組內等實作細節以 `ui-plan`／`api-plan` 為準；Gherkin 不綁定跨組規則原文。

